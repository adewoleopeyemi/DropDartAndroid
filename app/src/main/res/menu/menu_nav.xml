<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/nav_home"
        android:icon="@drawable/ic_home_back"
        android:iconTint="@color/colorwhite"></item>
    <item android:id="@+id/nav_profile"
        android:icon="@drawable/ic_profile_back"
        android:iconTint="@color/colorwhite"></item>

    <item android:id="@+id/video"
        android:icon="@drawable/ic_video_record"></item>

    <item android:id="@+id/nav_users"
        android:iconTint="@color/colorwhite"
        android:icon="@drawable/ic_users"></item>
    <item android:id="@+id/nav_chat"
        android:icon="@drawable/ic_chat_black"
        android:iconTint="@color/colorwhite"></item>
</menu>